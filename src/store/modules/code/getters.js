/* ============
 * Getters for the code module
 * ============
 *
 * The getters that are available on the
 * code module.
 */

export default {
    getNewEstimateStatus: state => state.newEstimateStatus,
    getNewCodeNumber: state => state.newCodeNumber,
    getNewCodeTitle: state => state.newCodeTitle,
    getNewId: state => state.newId,
};
