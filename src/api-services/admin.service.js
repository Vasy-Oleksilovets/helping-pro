import Axios from 'axios';

const RESOURCE_NAME = '/';

export default {
    /*getAdminProfile(data) {
    var token = localStorage.getItem("token"); //this.$store.getters.getToken;
    return Axios.post(RESOURCE_NAME + "api/admin/get_profile", data, {
      headers: { Authorization: "Bearer " + token }
    });
  },*/
};
